// line comments are not detected if they are not at the beginning of a line and there is non-whitespace anywhere before them.
template names do not resolve between files
renaming a template only renames the definition, not the call
